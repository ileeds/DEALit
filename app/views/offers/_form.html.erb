<%= form_with(model: offer, local: true) do |form| %>
  <% if offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :offer_user_id %>
  </div>

  <div class="field">
    <%= form.label :home_id %>
    <%= form.number_field :home_id, id: :offer_home_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
