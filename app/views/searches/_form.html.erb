<%= form_with(model: search, local: true) do |form| %>
  <% if search.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(search.errors.count, "error") %> prohibited this search from being saved:</h2>

      <ul>
      <% search.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :search_user_id %>
  </div>

  <div class="field">
    <%= form.label :details %>
    <%= form.text_field :details, id: :search_details %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
