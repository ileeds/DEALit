<%if logged_in? && current_user.id == home.user_id %>
  <%= link_to 'Edit', edit_home_path(id: home.id) %>
  <%= link_to 'Destroy', home_path(id: home.id), method: :delete, data: { confirm: 'Are you sure?' } %>
<%end%>
