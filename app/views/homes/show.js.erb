$container = $('#index'),

$modal = $('.modal'),
$modalBody = $('.modal .modal-body'),
$modalHeading = $('.modal .modal-heading');

$container.css ('overflow-y', 'hidden');
$modalHeading.html("<%=@home.address%>");
$modalBody.html("<%= escape_javascript(render @home) %>");

$modal.modal();
